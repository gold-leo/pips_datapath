v2.0 raw
# This is an automatically-generated PIPS machine code file

# 0x0000: nop              # First instruction does not execute
0a00a000 

# 0x0004: li $sp, 0xf800   # Loads a starting address for the stack
0e01f800 

# 0x0008: li  $a0, 212
020100d4 

# 0x000c: jal print_decimal_number
ff03005c 

# 0x0010: li  $a0, 377
02010179 

# 0x0014: jal print_decimal_number
ff03005c 

# 0x0018: j HALT
f001ff00 

# 0x001c: slt  $t0, $a0, $a1            # a < b
76203000 

# 0x0020: bne  $t0, $zero, end_rem      # if a < b then:
e6010030 

# 0x0024: sub  $a0, $a0, $a1            # a = a - b
12203000 

# 0x0028: slt  $t0, $a0, $a1            # a < b
76203000 

# 0x002c: beq  $t0, $zero, loop_rem     # if !(a < b), loop
d6010024 

# 0x0030: add  $v0, $zero, $a0          # Set return to remainder
01002000 

# 0x0034: jr   $ra                      # Return to caller
f000f000 

# 0x0038: addi $t1, $zero, 0            # reset t1
07010000 

# 0x003c: slt  $t0, $a0, $a1            # a < b
76203000 

# 0x0040: bne  $t0, $zero, end_quo      # if a < b then:
e6010054 

# 0x0044: sub  $a0, $a0, $a1            # a = a - b
12203000 

# 0x0048: slt  $t0, $a0, $a1            # a < b
76203000 

# 0x004c: addi $t1, $t1, 0x1            # counter++
07710001 

# 0x0050: beq  $t0, $zero, loop_quo     # if !(a < b), loop
d6010044 

# 0x0054: add  $v0, $zero, $t1          # Set counter to return value
01007000 

# 0x0058: jr   $ra                      # Return to caller
f000f000 

# 0x005c: li   $t3, TERMINAL            # Loads terminal address to $t3
0901ff10 

# 0x0060: bne  $a0, $zero, else_print   # If decimal number is 0, print '0' to terminal, else branch to 'else_print'
e2010070 

# 0x0064: li   $t0, 0x30                # Load ASCII value for '0'
06010030 

# 0x0068: sb   $t0, 0($t3)              # Print '0'
b6910000 

# 0x006c: jr   $ra
f000f000 

# 0x0070: addi $sp, $sp, -4             # Allocate space in the stack
0ee1fffc 

# 0x0074: sw   $a0, 0($sp)              # Storing $a0 to the stack
c2e10000 

# 0x0078: sw   $ra, 2($sp)              # Storing the original return address to the stack
cfe10002 

# 0x007c: li   $a1, 10                  # $a1 = 10
0301000a 

# 0x0080: jal  remainder                # Call remainder to calculate the remainder between n and 10
ff03001c 

# 0x0084: add  $s1, $zero, $v0          # int digit = n % 10
0b001000 

# 0x0088: lw   $a0, 0($sp)              # Loading $a0 from the stack
a2e10000 

# 0x008c: lw   $ra, 2($sp)              # Loading the original return address from the stack
afe10002 

# 0x0090: addi $sp, $sp, 4              # Deallocate space in the stack
0ee10004 

# 0x0094: sub  $t2, $s1, $a0            # $t2 = digit - n
18b02000 

# 0x0098: slt  $t2, $t2, $zero          # Checks if $t2 < 0
78800000 

# 0x009c: addi $sp, $sp, -4             # Allocate space in the stack
0ee1fffc 

# 0x00a0: sw   $s1, 0($sp)              # Storing the 'digit' to the stack
cbe10000 

# 0x00a4: sw   $ra, 2($sp)              # Storing the original return address to the stack
cfe10002 

# 0x00a8: li   $a1, 10                  # $a1 = 10
0301000a 

# 0x00ac: beq  $t2, $zero, exit_print   # If $t2 = 0, then branch to exit_print
d80100bc 

# 0x00b0: jal  quotient                 # Call quotient to divide n/10
ff030038 

# 0x00b4: add  $a0, $zero, $v0          # Set the parameter for the recursion call to 'digit'
02001000 

# 0x00b8: jal  print_decimal_number     # Recurse call to print_decimal_number
ff03005c 

# 0x00bc: lw   $ra, 2($sp)              # Loading the original return address from the stack
afe10002 

# 0x00c0: lw   $s1, 0($sp)              # Loading $s0 from the stack
abe10000 

# 0x00c4: addi $sp, $sp, 4              # Deallocate space in the stack
0ee10004 

# 0x00c8: addi $t0, $s1, 0x30           # $t0 = 0 + digit
06b10030 

# 0x00cc: sb   $t0, 0($t3)              # Print '0 + digit'
b6910000 

# 0x00d0: jr   $ra                      # Return to Caller
f000f000 
